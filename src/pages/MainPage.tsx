import React from 'react';
import SideBar from "../widgets/sidebar/ui/SideBar";

const MainPage = () => {
    return (
        <div>
            <SideBar/>
        </div>
    );
};

export default MainPage;